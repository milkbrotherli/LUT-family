# Data

This directory is a copy of the local data tree used by the training and test scripts. It includes the available training/evaluation inputs and generated cache files so the repository can be exercised immediately after download.

The data tree is stored with Git LFS because several YUV files exceed 100 MB. Confirm the redistribution terms of every included source dataset before making the repository public.
